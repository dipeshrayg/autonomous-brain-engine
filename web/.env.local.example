# Copy to .env.local for local dev (.env.local is gitignored).
# The anon key is safe to expose in the browser — RLS bounds it.
VITE_SUPABASE_URL=https://gtmubkrzrjzkpnulvdup.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
